<UserControl
    x:Class="Kimono.Controls.MasterDetailViewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kimono.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="thisControl"
    d:DesignHeight="1080"
    d:DesignWidth="1920">

    <Grid x:Name="LayoutRoot">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="TwoPaneVisualState">
                    <VisualState.Setters>
                        <Setter Target="masterViewContentControl.Visibility" Value="Visible" />
                        <Setter Target="masterViewContentControl.Width" Value="400" />
                        <Setter Target="masterViewContentControl.(RelativePanel.AlignLeftWithPanel)" Value="True" />

                        <Setter Target="detailViewContentControl.Visibility" Value="Visible" />
                        <Setter Target="detailViewContentControl.(RelativePanel.AlignRightWithPanel)" Value="True" />
                        <Setter Target="detailViewContentControl.(RelativePanel.RightOf)" Value="masterViewContentControl" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="OnePaneMasterVisualState">
                    <VisualState.Setters>
                        <Setter Target="masterViewContentControl.Visibility" Value="Visible" />
                        <Setter Target="masterViewContentControl.Width" Value="NaN" />
                        <Setter Target="masterViewContentControl.(RelativePanel.AlignLeftWithPanel)" Value="True" />
                        
                        <Setter Target="detailViewContentControl.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="OnePaneDetailVisualState">
                    <VisualState.Setters>
                        <Setter Target="detailViewContentControl.Visibility" Value="Visible" />
                        <Setter Target="detailViewContentControl.Width" Value="NaN" />
                        <Setter Target="detailViewContentControl.(RelativePanel.AlignLeftWithPanel)" Value="True" />

                        <Setter Target="masterViewContentControl.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <RelativePanel x:Name="relativePanelParent">
            <ContentPresenter x:Name="masterViewContentControl" RelativePanel.AlignLeftWithPanel="True" 
                            VerticalContentAlignment="Stretch" VerticalAlignment="Stretch" Width="400" 
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                            Content="{Binding MasterViewPaneContent, ElementName=thisControl, UpdateSourceTrigger=PropertyChanged}"
                            Height="{Binding ElementName=relativePanelParent, Path=ActualHeight}" />
            
            <ContentPresenter x:Name="detailViewContentControl" RelativePanel.AlignRightWithPanel="True" 
                            RelativePanel.RightOf="masterViewContentControl" Width="NaN" 
                            VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" 
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                            Content="{Binding DetailViewPaneContent, ElementName=thisControl, UpdateSourceTrigger=PropertyChanged}"
                            Height="{Binding ElementName=relativePanelParent, Path=ActualHeight}" />
        </RelativePanel>
    </Grid>
</UserControl>
